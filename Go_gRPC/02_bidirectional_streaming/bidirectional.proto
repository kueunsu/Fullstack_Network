syntax = "proto3";

package bidipb;

option go_package = "bidirectional_streaming/bidipb";

service Bidirectional {
  // A Bidirectional streaming RPC.
  rpc GetServerResponse (stream Message) returns (stream Message) {}
}

message Message {
  string message = 1;
}
