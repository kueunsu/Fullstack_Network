syntax = "proto3";

package hellopb;                            // 추가

option go_package = "hello_grpc_example/hellopb";   // 추가

service MyService {
    rpc MyFunction(MyNumber) returns (MyNumber) {}
}

message MyNumber {
    int32 value = 1;
}
